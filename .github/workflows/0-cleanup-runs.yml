name: A Cleanup Workflow Runs

on:
  # 每 2 天凌晨 3 点清理
  schedule:
    - cron: '0 3 */2 * *'
  
  # 手动触发
  workflow_dispatch:

permissions:
  actions: write
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Delete old workflow runs
      uses: Mattraks/delete-workflow-runs@v2
      with:
        token: ${{ github.token }}
        repository: ${{ github.repository }}
        retain_days: 0
        keep_minimum_runs: 1
        delete_workflow_by_state_pattern: 'ALL'
        delete_run_by_conclusion_pattern: 'ALL'
    
    - name: Generate README.md with run status
      run: |
        cat > README.md << EOF
        # Workflow Run Status
        
        | 项目 | 值 |
        |------|-----|
        | 工作流名称 | ${{ github.workflow }} |
        | 运行编号 | #${{ github.run_number }} |
        | 运行时间 | $(TZ='Asia/Shanghai' date '+%Y-%m-%d %H:%M:%S') |
        | 触发方式 | ${{ github.event_name }} |
        | 运行状态 | ✅ 成功 |
        EOF
    
    - name: Commit and push README.md
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add README.md
        git diff --staged --quiet || git commit -m "📝 Update README.md - Run #${{ github.run_number }}"
        git push
